node_modules/
/test-results/
/playwright-report/
/playwright/.cache/
/.auth/
*.log
.DS_Store
.env
.env.local

# Loom walkthrough — cheat sheet + full prep + any recorded video.
# Not required to run the suite; kept locally for the demo only.
LOOM_SCRIPT.md
LOOM_CONTEXT.md
*.mp4
*.mov
*.webm

# Safety belt: deploy-key keypair files if anyone runs the
# `ssh-keygen` recipe from docs/SETUP.md inside this directory
# instead of /tmp. Never commit a private key.
/sut_deploy
/sut_deploy.pub
*.pem
id_ed25519*
id_rsa*
